public interface Render {
    void renderIcon(Screen screen);
    void renderHost(Screen screen);
    void renderBar(Screen screen);
    void renderEvent(Screen screen);
    void renderTimePlace(Screen screen);
    void renderWelcome(Screen screen);
}